server:
  port: 9000
spring:
  threads:
    virtual:
      enabled: false
  datasource:
    url: jdbc:h2:mem:db_test;Mode=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create